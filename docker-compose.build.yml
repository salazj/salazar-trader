# Use this file to build images from source instead of pulling from GHCR.
# Usage: docker compose -f docker-compose.yml -f docker-compose.build.yml up --build

services:
  backend:
    build: .

  frontend:
    build: ./frontend

  bot:
    build: .

  backtest:
    build: .

  train:
    build: .
